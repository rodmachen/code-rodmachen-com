Do each step separately and ask for user confirmation to move to the next step

### Step 1

Update planning do to be more explicit about which parts of Step 7 should be done in what order.

As far as the deployment plan, I currently have a running code.rodmachen.com. However it's being served from an S3 bucket. I think we can can go ahead and name this new vercel instance code.rodmachen.com. Any reason not to? And is code-rodmachen-com a better name? 

### Step 2 - UI Changes
1. Let's take all of these and create a step or steps between 6 and 7. Ask questions and then update the plan.
1. There are two menu bars and some odd extra white border on the top and left.
3. Window centered, full height (below menu bar), using a defined maximum width of 1000px.
5. When doing a click and drag on the window, the window goes invisible with just a thin border upon initial click.
6. Remove seconds from clock. Is this the user's local time?
7. Apple menu → "About This Site" → modal with tech stack info
7. No keyboard shortcuts in any menu bar.
7. File menu → Open... which opens the post listings page
8. Edit menu → disabled "Edit Posts" item (grayed out, not clickable)
9. View menu → "Full Width" / "Normal" items that toggle the zoom state
10. Help menu → "Help me…" item that opens `https://www.google.com` in a new tab
11. sound (speaker icon) should be to the left of the clock and default to muted.
1. There should be documents on the desktop for About and Contact.
12. When opened, About and Contact become smaller sub-windows that open on top of the main window.
13. Let's move to a separate window the post listings. Post window should be post only. 
1. The post listings could maybe look like the current file list that has Filename, File Type, and Size. Instead:
    - Name
    - Date Added
    - Tags (can have multiple)
1. Macintosh HD -> Hard Drive
1. The only fonts in the posts should be Chicago and Geneva. All headers Chicago, including the Title.

